# ⚠ السبب الأرجح لتعطّل الخدمة: الملف الأصلي كان بدون أي إصدار مثبّت.
# يعني كل إعادة بناء تسحب آخر نسخة من كل مكتبة — واحدة منها تصدر
# تحديثاً كاسراً، والخدمة تنهار بدون ما تغيّر أنت سطر واحد بالكود.
#
# الخطر الأكبر هنا هو ثنائي numpy + opencv: حزم OpenCV المبنية على
# numpy 1.x تنهار عند الاستيراد مع numpy 2.x برسالة
# "numpy.core.multiarray failed to import" — والانهيار يصير عند
# الإقلاع، فالحاوية ما تفتح المنفذ أبداً والمستضيف يرجّع 503.

fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
python-multipart>=0.0.9
opencv-python-headless>=4.10,<5.0
pytesseract>=0.3.10,<0.4
mrz>=0.6,<1.0
numpy>=1.26,<3.0

# ══════════════════════════════════════════════════════════════════
# بعد أول بناء ناجح، ثبّت الإصدارات بالضبط حتى ما تتكرر المشكلة:
#
#   pip freeze > requirements.lock.txt
#
# وبدّل الملف بالناتج. الحدود أعلاه تحميك من الكسر الكبير، بس
# التثبيت الدقيق هو اللي يضمن إن البناء اليوم = البناء بعد سنة.
# ══════════════════════════════════════════════════════════════════
